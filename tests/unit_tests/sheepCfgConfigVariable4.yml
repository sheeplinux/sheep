bootloader:
  mode:
  image: http://172.19.118.1/qcow2/os-deploy-bootloader.tar.gz
  kernel_parameter:
    console:
      serial:
      baudRate:

linux:
  image: http://172.19.118.1/squashfs/disco.squashfs
  label:
  device:
  selinux:

network:
  interface: ens1
  ipAdr_serverPxe:

pxePilot:
  enable: false
  url:
  port:
  config_after_reboot:
